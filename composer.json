{
    "name": "inlayphp/inlay",
    "description": "PHP-first administration panels and UI components for Laravel and Inertia.",
    "type": "library",
    "license": "MIT",
    "repositories": [
        {
            "type": "path",
            "url": "packages/*",
            "options": {
                "symlink": true
            }
        }
    ],
    "require": {
        "php": "^8.3",
        "illuminate/console": "^12.0 || ^13.0",
        "illuminate/database": "^12.0 || ^13.0",
        "illuminate/filesystem": "^12.0 || ^13.0",
        "illuminate/support": "^12.0 || ^13.0",
        "illuminate/validation": "^12.0 || ^13.0",
        "inlayphp/actions": "^0.3 || dev-main",
        "inlayphp/authorization": "^0.3 || dev-main",
        "inlayphp/core": "^0.3 || dev-main",
        "inlayphp/design": "^0.3 || dev-main",
        "inlayphp/forms": "^0.3 || dev-main",
        "inlayphp/infolists": "^0.3 || dev-main",
        "inlayphp/media": "^0.3 || dev-main",
        "inlayphp/media-manager": "^0.3 || dev-main",
        "inlayphp/notifications": "^0.3 || dev-main",
        "inlayphp/panels": "^0.3 || dev-main",
        "inlayphp/resources": "^0.3 || dev-main",
        "inlayphp/schemas": "^0.3 || dev-main",
        "inlayphp/support": "^0.3 || dev-main",
        "inlayphp/tables": "^0.3 || dev-main",
        "inlayphp/theme": "^0.3 || dev-main",
        "inlayphp/validation": "^0.3 || dev-main",
        "inlayphp/widgets": "^0.3 || dev-main",
        "symfony/process": "^7.2 || ^8.0"
    },
    "require-dev": {
        "inlayphp/authorization-spatie": "^0.3 || dev-main",
        "inlayphp/imports": "^0.3 || dev-main",
        "inlayphp/media-spatie": "^0.3 || dev-main",
        "inlayphp/permission-manager": "^0.3 || dev-main",
        "inlayphp/tables-xlsx": "^0.3 || dev-main",
        "inlayphp/two-factor-authentication": "^0.3 || dev-main",
        "phiki/phiki": "^2.0",
        "pestphp/pest": "^4.0"
    },
    "suggest": {
        "inlayphp/authorization-spatie": "Synchronize Inlay abilities with Spatie Laravel Permission.",
        "inlayphp/imports": "Add validated, fault-isolated import workflows.",
        "inlayphp/media-spatie": "Bridge the Inlay media catalog with Spatie Media Library.",
        "inlayphp/permission-manager": "Add role, permission, assignment, and access-audit screens.",
        "inlayphp/tables-xlsx": "Add an optional PhpSpreadsheet XLSX export driver for Inlay tables.",
        "inlayphp/two-factor-authentication": "Add optional TOTP, recovery-code, enrollment, and panel challenge flows."
    },
    "autoload": {
        "psr-4": {
            "Inlay\\Installer\\": "src/"
        }
    },
    "autoload-dev": {
        "classmap": [
            "tests/Fixtures"
        ]
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "extra": {
        "laravel": {
            "providers": [
                "Inlay\\Installer\\InlayServiceProvider"
            ]
        }
    },
    "config": {
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "test": "pest",
        "lint": "find src packages tests -name '*.php' -print0 | xargs -0 -n1 php -l"
    }
}
